<UserControl x:Class="VAC.Views.CharactersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VAC.Views"
             xmlns:vm="clr-namespace:VAC.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:CharactersViewModel/>
    </UserControl.DataContext>

    <StackPanel Orientation="Horizontal">
        <Label Content="Character:"/>
        <Label Content="{Binding Character.Name}"/>

        <Border CornerRadius="90" BorderThickness="2" BorderBrush="White" Width="200" Height="200">
            <Border.Clip>
                <EllipseGeometry Center="100,100" RadiusX="90" RadiusY="90"/>
            </Border.Clip>
            <Button Command="{Binding GenerateCommand}" Background="Transparent" BorderThickness="0">
                <Button.Content>
                    <Image Source="{Binding Character.Splash}" Stretch="UniformToFill"/>
                </Button.Content>
            </Button>
        </Border>

    </StackPanel>
</UserControl>
